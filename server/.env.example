# === 서버 기본 ===
HOST=127.0.0.1
PORT=8000

# === 세션/쿠키 ===
SESSION_SECRET=change_me_to_long_random_string
COOKIE_NAME=authmin_session
COOKIE_SAMESITE=lax
COOKIE_SECURE=false
# COOKIE_DOMAIN=            # 로컬(127.0.0.1)은 비워두는 게 안전. 필요할 때만 설정

# === MongoDB ===
MONGO_URI="mongodb+srv://yongsang:oQeYKFzKz5Y0xdBu@cluster0.g0bta9q.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0"
DB_NAME=MFAlogin

# === CORS ===
# 프론트/백엔드 모두 127.0.0.1로 통일했으므로 여기서도 127로만 허용
CORS_ORIGINS=http://127.0.0.1:8000,http://127.0.0.1:5500

# === 토큰/링크 ===
SECRET_KEY=change_me_long_random
SITE_BASE_URL=http://127.0.0.1:8000
EMAIL_TOKEN_EXPIRE_HOURS=24

# === SMTP (옵션: 비워두면 콘솔 출력) ===
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_STARTTLS=false
SMTP_USER=20211884@edu.hanbat.ac.kr
SMTP_PASS=gtcj kqkf yvdj feks
SMTP_FROM=20211884@edu.hanbat.ac.kr

# === TOTP(OTP) 설정 ===
# * totp_service.py는 TOTP_ISSUER / TOTP_PERIOD / TOTP_WINDOW / BACKUP_CODE_COUNT / BACKUP_CODE_LEN을 봅니다.
TOTP_ISSUER=ZeroTrust         # ← 기존 ISSUER_NAME 대신 이 이름을 사용하세요
TOTP_PERIOD=30                # 30초 타임스텝
TOTP_WINDOW=1                 # ±1스텝 허용
BACKUP_CODE_COUNT=8           # 백업코드 개수
BACKUP_CODE_LEN=10            # (선택) 백업코드 길이
# 백업코드 해시 페퍼는 totp_service가 SESSION_SECRET → SECRET_KEY 순으로 사용하므로
# BACKUP_CODE_PEPPER는 필요 없습니다 (삭제/무시).

# === 디바이스 신뢰 쿠키(devtk) ===
DEVICE_COOKIE_NAME=devtk
DEVICE_COOKIE_SAMESITE=lax
DEVICE_COOKIE_SECURE=false
DEVICE_TOKEN_EXPIRE_DAYS=180
